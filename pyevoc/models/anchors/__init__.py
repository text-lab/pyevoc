"""Anchor word lists for thematic filtering.

Each ``.txt`` file in this directory contains one anchor term per line.
The files are loaded at runtime by :mod:`pyevoc.preprocessing.thematic_filtering`
to restrict the corpus to a target semantic domain.
"""
